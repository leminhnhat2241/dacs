services:
  - type: web
    name: e-sport-service
    runtime: dotnet
    buildCommand: dotnet publish -c Release -o out
    startCommand: dotnet out/E_Sport.dll
    env:
      - key: ASPNETCORE_URLS
        value: http://0.0.0.0:10000
    plan: free
