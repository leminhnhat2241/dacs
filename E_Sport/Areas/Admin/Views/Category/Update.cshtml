@* @model E_Sport.Models.Category

@{
    ViewData["Title"] = "Update";
    Layout = "~/Areas/Admin/Views/_AdminLayout.cshtml";
}

<h1>Chỉnh sửa</h1>

<h4>Loại sản phẩm</h4>
<hr />
<form asp-action="Update">
    <input type="hidden" asp-for="Id" />
    <div class="form-group">
        <label asp-for="Name">Tên loại</label>
        <input asp-for="Name" class="form-control" />
    </div>
    <button type="submit" class="btn btn-warning">Xong</button>
    <a asp-action="Index" class="btn btn-secondary">Quay lại</a>
</form>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
 *@


 @model E_Sport.Models.Category

@{
    ViewData["Title"] = "Update";
    // Layout = "~/Areas/Admin/Views/_AdminLayout.cshtml";
}

<h2 class="text-center mb-4">Chỉnh sửa loại sản phẩm</h2>

<hr />

<form asp-action="Update">
    <input type="hidden" asp-for="Id" />

    <div class="form-group mb-3">
        <label asp-for="Name" class="form-label">Tên loại</label>
        <input asp-for="Name" class="form-control" placeholder="Nhập tên loại sản phẩm" />
        <span asp-validation-for="Name" class="text-danger"></span>
    </div>

    <div class="d-flex justify-content-between">
        <button type="submit" class="btn btn-warning">Xong</button>
        <a asp-action="Index" class="btn btn-secondary">Quay lại</a>
    </div>
</form>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}
